export const ROUTES = {
  LOGIN: "/login",
  LOGOUT: "/logout",
  AI_DEPLOYMENTS: "/ai-deployments",
  ARCHITECTURES: "/architectures",
  SERVICES: "/services",
  USE_CASE_REFERENCES: "/use-case-references",
} as const;
