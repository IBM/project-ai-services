// Package ingestion is retained as a placeholder. The legacy pod-wait logic
// (WaitForAllPodsHealthy / areRequiredPodsHealthy) and CLI-based ingestion path
// have been removed. All ingestion is now handled via the digitize microservice
// API — see tests/e2e/digitization for all ingestion/digitization tests.
package ingestion
