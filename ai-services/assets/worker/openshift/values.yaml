worker:
  image: icr.io/ai-services-cicd/ai-services:v0.0.292
  runtime: "openshift"
  adminPasswordHash: ""
  # @generate:password length=32, special=false
  dbEncryptionKey: ""
  resources:
    requests:
      memory: "1Gi"
      cpu: "250m"
    limits:
      memory: "1Gi"
      cpu: "1000m"

