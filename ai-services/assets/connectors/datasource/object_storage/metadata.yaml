# Connector identification
type: connector
id: object_storage
name: "Object Storage"
description: "Connect to an S3-compatible object storage bucket as a document source for ingestion pipelines."

# Connector type this provider belongs to
connector_type: datasource
connector_name: "Data source"
