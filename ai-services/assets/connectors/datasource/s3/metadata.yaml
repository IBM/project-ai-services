# Connector identification
type: connector
id: s3
name: "Amazon S3 / S3-Compatible Object Storage"
description: "Connect to an S3-compatible object storage bucket as a document source for ingestion pipelines."

# Connector type this provider belongs to
connector_type: datasource
connector_type_name: "Datasource"

# Fields that contain sensitive credential values.
# These values are encrypted at rest and never returned in API responses.
sensitive_fields:
  - secret_access_key
