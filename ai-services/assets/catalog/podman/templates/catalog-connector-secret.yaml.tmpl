apiVersion: v1
kind: Secret
metadata:
  name: catalog-connector-secret
  labels:
    ai-services.io/application: "{{ .AppName }}"
    ai-services.io/template: "{{ .AppTemplateName }}"
    ai-services.io/version: "{{ .Version }}"
type: Opaque
stringData:
  connector-encryption-key: "{{ .Values.backend.connectorEncryptionKey }}"
