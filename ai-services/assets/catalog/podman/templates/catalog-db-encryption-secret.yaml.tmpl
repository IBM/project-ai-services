apiVersion: v1
kind: Secret
metadata:
  name: catalog-db-encryption-secret
  labels:
    ai-services.io/application: "{{ .AppName }}"
    ai-services.io/template: "{{ .AppTemplateName }}"
    ai-services.io/version: "{{ .Version }}"
type: Opaque
stringData:
  db-encryption-key: "{{ .Values.backend.dbEncryptionKey }}"
