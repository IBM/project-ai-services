apiVersion: v1
kind: Secret
metadata:
  name: catalog-mtls-encryption-secret
  labels:
    ai-services.io/application: "{{ .AppName }}"
    ai-services.io/template: "{{ .AppTemplateName }}"
    ai-services.io/version: "{{ .Version }}"
type: Opaque
stringData:
  mtls-encryption-key: "{{ .Values.backend.mtlsEncryptionKey }}"
