apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "{{ .Release.Name }}--cache"
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-storage-provisioner
  resources:
    requests:
      storage: "10Gi"
