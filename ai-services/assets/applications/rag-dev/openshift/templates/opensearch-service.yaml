apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}--opensearch"
  labels:
    ai-services.io/application: {{ .Release.Name }}
    ai-services.io/template: {{ .Chart.Name }}
    ai-services.io/version: {{ .Values.app.version | default .Chart.AppVersion | quote }}
spec:
  selector:
    ai-services.io/application: {{ .Release.Name }}
  ports:
    - name: os-client-port
      port: 9200
      targetPort: 9200
      protocol: TCP
    - name: os-metrics-port
      port: 9600
      targetPort: 9600
      protocol: TCP
  type: ClusterIP
