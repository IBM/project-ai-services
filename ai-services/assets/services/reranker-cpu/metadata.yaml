# Service identification
id: reranker-cpu
name: "Reranker CPU"
description: "CPU-based document reranking service for improved search relevance"
type: service

# Certification and support
certified_by: "IBM"

# Service belongs to these architectures
architectures:
  - rag-dev

# This service is only used as a dependency by other services
dependency_only: true

# Made with Bob
