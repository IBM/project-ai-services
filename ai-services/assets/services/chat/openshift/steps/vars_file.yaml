containers:
  - name: "ui"
    format: ".Status"
    alias: UI_STATUS

  - name: "backend-server"
    format: ".Status"
    alias: BACKEND_STATUS

hosts:
  - fetch: UI_ROUTE
    type: route
  - fetch: BACKEND_ROUTE
    type: route
