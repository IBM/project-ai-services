pods:
  - name: "{{ .AppName }}--chat-bot-dev"
    format: "index .Ports \"3000/tcp\" 0"
    default: ""
    alias: UI_PORT

  - name: "{{ .AppName }}--chat-bot-dev"
    format: "index .Ports \"5000/tcp\" 0"
    default: ""
    alias: BACKEND_PORT

containers:
  - name: "{{ .AppName }}--chat-bot-dev-ui"
    format: ".Status"
    alias: UI_STATUS

  - name: "{{ .AppName }}--chat-bot-dev-backend-server"
    format: ".Status"
    alias: BACKEND_STATUS

hosts:
  - fetch: HOST_IP
    type: ip

# Made with Bob
