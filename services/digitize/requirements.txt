## Digitize-service specific dependencies
##
## S3 connector dependencies — boto3, botocore, and s3transfer are pure-Python
## and fully compatible with ppc64le.  Pin all three to the same release cadence;
## boto3 and botocore share versions, s3transfer is boto3's multipart-transfer
## engine and must be pinned separately.
boto3==1.38.0
botocore==1.38.0
s3transfer==0.12.0
paramiko==5.0.0
## APScheduler v4 — AsyncScheduler + AsyncSQLAlchemyDataStore for connector scheduling.
## sqlalchemy extra pulls in the async store support.
apscheduler[sqlalchemy]==4.0.0a6
