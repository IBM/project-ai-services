diff --git a/pyproject.toml b/pyproject.toml
index 753f6ae2dd..3d78223aff 100644
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -48,7 +48,7 @@ proxy = [
     "granian>=2.7.4,<3.0",
     "uvloop>=0.22.1,<1.0; sys_platform != 'win32'",
     "fastapi>=0.136.3,<1.0",
-    "starlette>=1.0.1,<2.0",
+    "starlette>=1.3.1,<2.0",
     "backoff>=2.2.1,<3.0",
     "pyyaml>=6.0.3,<7.0",
     "rq>=2.7.0,<3.0",
@@ -59,7 +59,7 @@ proxy = [
     "apscheduler>=3.11.2,<4.0",
     "fastapi-sso>=0.19.0,<1.0",
     "PyJWT>=2.13.0,<3.0",
-    "python-multipart>=0.0.27,<1.0",
+    "python-multipart>=0.0.30,<1.0",
     "cryptography>=49.0.0,<51.0",
     "pynacl>=1.6.2,<2.0",
     "websockets>=15.0.1,<16.0",
diff --git a/litellm/proxy/pass_through_endpoints/pass_through_endpoints.py b/litellm/proxy/pass_through_endpoints/pass_through_endpoints.py
index be669502..e63c75d0 100644
--- a/litellm/proxy/pass_through_endpoints/pass_through_endpoints.py
+++ b/litellm/proxy/pass_through_endpoints/pass_through_endpoints.py
@@ -1590,6 +1590,16 @@ async def pass_through_request(
                     request.method,
                 )
 
+        response_body = await proxy_logging_obj.post_call_success_hook(
+            data=kwargs,
+            user_api_key_dict=user_api_key_dict,
+            response=response_body,  # type: ignore[arg-type]
+        )
+        post_hook_content = json.dumps(response_body).encode("utf-8")
+        if content != post_hook_content:
+            content = post_hook_content
+            _content_modified = True
+
         ## LOG SUCCESS
         # Upstream errors are already logged via _log_passthrough_upstream_failure
         # above; the success handler has no status-code awareness of its own; so
